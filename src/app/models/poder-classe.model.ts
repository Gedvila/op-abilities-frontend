import { BaseItem } from './base-item.model';

export interface PoderClasse extends BaseItem {
  // TODO: adicione os campos do AbilityResponseDTO do seu backend
  // Exemplos comuns para habilidades de arquétipo (Ordem Paranormal):
  // archetype?: { id: number; name: string };  // arquétipo/classe
  // level?:     number;                         // nível necessário
  // prereq?:    string;                         // pré-requisito
}
