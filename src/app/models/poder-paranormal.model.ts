import { BaseItem } from './base-item.model';

export interface PoderParanormal extends BaseItem {
  // TODO: adicione os campos do ParanormalPowerResponseDTO do seu backend
  // Exemplos comuns para poderes paranormais (Ordem Paranormal):
  // element?:       { id: number; name: string };
  // executionType?: string;   // tipo de execução (ação completa, movimento...)
  // cost?:          number;   // custo em PE
  // range?:         string;   // alcance
}
